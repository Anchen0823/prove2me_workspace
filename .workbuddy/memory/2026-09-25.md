# 2026-09-25

## 01:49 收尾：`d5c69ba9` 的提交监视器跑满 2 小时，全部 404

- `tmp/submit_product_log_large.py`（21:34 启动，720 次尝试 × 10 s）**每一次都是 404**，
  01:49 正常放弃，**没有产生任何提交**（无 `verification/product-log-large-verdict.json`）。
  日志：`verification/product-log-large-watch.log`；23:06:31 的盲发 POST 也回
  `{"error":"Theorem d5c69ba9-... not found"}`。
- ⇒ 结论钉死：`TaoFivePrimes.rosser_schoenfeld_product_log_bound_large`
  (`d5c69ba9-5a40-495a-a728-e7d11107a438`) 的**那一行在平台上持续解析不了**（~4 h），
  **不是抽风**。但它在全树遍历里一直存在（`Open`、未 deprecated），且父节点
  `fce5d444` 的两条已接受分解都仍把它列为孩子 ⇒ 义务真实、边也在。
- 因此这份约简（`A* + B`，文件 `Solutions/Sol_TaoFivePrimes_rosser_schoenfeld_product_log_bound_large.lean`，
  签名已逐字核对）**只能在平台修复后再交**；脚本可直接重跑，无需改动。
- 上一轮（2026-09-24 23:30–24:00）的成果见 `2026-09-24.md`：新节点
  `TaoFivePrimes.mawia_reciprocal_sum_bound`（`a182289a`）+ `A`/`A*` 两条约简均
  **SKETCH_ACCEPTED**（`512a35d6` / `ebbdeec3`）。

## 10:5x ζ(9) mission：把 formalisable 层接进平台 DAG

**起点**：根节点 `ZetaNine.irrational_zeta_nine`（`523f37b8`，Private/Open）在平台
dependency graph 里**一个孩子都没有**（`/theorems/523f37b8/decompositions` → `[]`），
mission 说明写满了箭头但图里没边。

**平台版本先动了**：`/agent/refresh` 回 `0.11.1`（旧记录是 `0.10.9`），
`sync_private_proposal.py` 的版本守卫直接挡下取 token ⇒ 必须先复审文档。0.11.1 的实际
delta：① 队长文档把 faithfulness 展开成 9 条细则（读整节而非单个定理、双向匹配假设与结论、
total function 的坏输入默认值、按源的集合量化、边界输入、手验公式、定义优先、regime change
复查、**证明难度不是你的事**）；② proposal 新增 `Changes requested` 状态（可改可重提）；
③ 新增 mission 级 `POST /missions/:id/make-public`（**本次没调用**，要用得先问宇轩）。
本地 `references/mission_captain.md`/`mission_solver.md` 抄了上游新版，`EXPECTED_VERSION`
改成 `0.11.1`，`credentials.json` 里过期 token 重刷。

**成果**（回执 `roadmap/platform/wiring/wiring-receipt.json`，说明 `wiring/README.md`）：
- 5 个新 private 节点：`…irrational_of_small_nonzero_integer_forms`（TP2，Proved）、
  `…taylor_sign_implies_kernel_sum_pos`（TP1，Proved）、`…quadrature_exact_of_moments`
  （FQ1，Proved）、`…exponentially_small_nonzero_forms_of_zeta_nine`（R1，Open）、
  `…exponentially_small_independent_forms_of_zeta_nine`（R2，Open）。
- 根节点两条分解：`SKETCH_ACCEPTED` `42bd3e13`（单形式）/ `0da22fdd`（双形式）。
- 里程碑 35 → 40；mission 说明追加一节（`25885` 字符）；讨论区评论 `7463b390`。
- 3 个 Proved 节点的证明 `ACCEPTED`；本地 5 份 solution 全部 `lake env lean` 通过，
  4 个 `Theorems/Thm_ZetaNine_*.lean` 镜像 `lake build` 通过。
- 盲审：5 条陈述全部 `FAITHFUL AND WELL-POSED`（报告 `wiring/readback-2026-09-25.md`）。

**两条方法论教训（已进 MEMORY.md）**：
① 只写「存在小整数形式」的孩子一旦有准则就与目标**等价** ⇒ 伪装的重述；要严格更强必须钉
**固定指数速率**。② 带自由参数的父节点无法 import 封闭的孩子定理 ⇒ 多级图必须先有定义层。
⇒ 算术箭头 `V←VA,Q,X` 等**故意没发**，根仍 Open。

**踩的坑**：`wiring/publish_wiring.py --poll-jobs` 第一版打印了 PUBLISHED 却**漏了
`save(data)`**，导致 `--prove` 看到 PENDING 直接抛错；补一行即修。另 `update_description.py`
的尾部断言写错（以为描述以 `- S. Suman` 结尾，实际以 arXiv 链接行结尾），守卫正确挡下了，
改成检查 `2609.22316`。**守卫挡下来是好事，别为了跑通把守卫放宽。**

## 11:3x ζ(9) 第二批：再上 4 个「笔记已证」节点（TP/FI/TA）

脚本 `wiring/publish_notes_layer.py`，回执 `wiring/notes-layer-receipt.json`：

| 节点 | 分量 | 状态 | 孩子边 |
|---|---|---|---|
| `ZetaNine.five_sample_sign_forces_nonzero` | TP3 | Proved | ← `quadrature_exact_of_moments` |
| `ZetaNine.min_lt_weighted_average_lt_max` | FI1 | Proved | — |
| `ZetaNine.mediant_strictly_between_min_and_max` | FI2 | Proved | ← `min_lt_weighted_average_lt_max` |
| `ZetaNine.positive_matrix_maps_nonneg_to_pos` | TA1 | Proved | — |

里程碑 40 → **44**（挂定理 11 → **15**），平台多登记 **2 条 decomposition 边**。四份解本地
`lake env lean` 全过；4 条陈述盲审全 `FAITHFUL AND WELL-POSED`（含专门追问「调和平均项
分母会不会是 0」——`hb : 0 < b` 已保证，且 `Σ w·b > 0` 可证）。mission 说明追加
`### Notes-layer batch`（28156 字符）、讨论区评论 `412f3b6b`。

**这一批最贵的教训**（已写进 `LEAN-PITFALLS.md`）：mediant 第一版被平台判 `CE`，两个原因
① **本环境 `rw` 不往 `∑` 绑子里重写**——`rw [div_eq_mul_inv, ← Finset.sum_mul]` 只改了
等式的一侧，报「找不到模式」而打印出来的目标里 `∑` 里明明还有 `/B`；改用
`simp only [hv, div_eq_mul_inv, ← Finset.sum_mul, ← hB]`（`simp` 会进绑子且支持 `←`）一次通过。
② `set B := … with hB` **已经**把目标里的同名项折成 `B`，所以之后 `rw [← hB]` 报「找不到」——
那一步根本不该写。
③ **别在本地编译还没跑完时就提交**：第一版就是趁编译在跑时交的，平台报的错和编译器报的
一模一样，白花一轮。`publish_notes_layer.py` 现在会保留被拒提交的 `history` 并自动重试。

**方向判断（本轮确认）**：笔记里剩下还**能**不依赖具体构造形式化的只剩
GO/FM/XL/CM 这类「抽象对照模型」和 FO 的逼近框架，价值不如已做的；**再往上必须补定义层**
（$F_n$、饱和核、Smith 数据、加权面积、同余格极小值），否则算术箭头 `V←VA,Q,X` 等发不出来。

## 11:0x ζ(9)：全语料证明态审计（九轮 + roadmap + 平台层）

**产物**（宇轩要的「整理 zeta9 成果」）：
- `missions/zeta9/research/zeta9-proof-state-audit.tex` — 英文 LaTeX 正本，1229 行，13 个表 + 2 张 TikZ DAG。
  Part I = 第一轮；Part II = 第 2–9 轮 + roadmap DAG + 平台层 + 新增缺口 + C1–C10 收口清单。
  顶部显式声明「除非每条依赖都闭合，否则不主张任何无理性结论」，**全程未修补任何缺口**。
- `missions/zeta9/research/zeta9-proof-state-audit-zh.md` — 中文摘要（状态标签 / 九轮一览 / roadmap 节点 /
  平台层 / 独立复核 / 六项指定审计 / C1–C10）。
- `missions/zeta9/status.md` 加了审计指针。
- 审计脚本+日志归档到 `missions/zeta9/verification/audit-2026-09-25/`（5 个脚本 + 5 份输出）。

**关键结构性结论**（可靠，可引用）：
① Part I 解析侧只给 `|L|` **上界**、算术侧只控**分母** ⇒ 该路线**没有**产生 `exp(−cn)` 的机制；
② roadmap 40 节点 = 13 open + 27「笔记已证」（**无 Lean 定义、无平台定理**）；所有收口命题都是 `∀`，
   语料只有 5 个尺度的有限证书；算术侧最紧统一估计 `22.337` vs 要求 `0.61071437`；
③ 平台 8 条已接受定理全是**抽象**陈述，具体对象（F_n/K_n/Smith/加权面积/同余格极小值）无 Lean 定义，
   所以 `V←VA,Q,X` 等算术箭头**故意没接线**（两条理由见 `wiring/README`）。

**独立复核（我做的，不是抄它的脚本）**：A1 `(9,2,0)` 从零重算 `L=7.89110563021818797780073476895…`
（与归档直接求和证书包络一致）；A2 轮廓定向符号 `−I/(2πi)=L` ⇒ **(C1) 负号正确**；
A5/A6 `Γ(α)` 6/6 在归档区间内、`sup H_α` 6/6 与归档一致到 12 位；A8 `t♯=d_n⁹/G` 在 5 组参数
（含 `n=224,m=48`）从零复核通过；A9 大素数 `v_p(G)` 公式 216 参数 / 1951 素项 0 失败；
**A10 从构造独立推出 round5–7 相位，得 `x_*=1.00297646575489808`、`f(x_*)=−10.5641538376402`、
`a=330.65816828687`、`√(3/a)=0.09525131927`，逐位复现笔记常数**（且 `−f_*−τ = 1.54×10⁻⁴` 是全部余量）；
**A11 FQ 五个预定点在 `n≤1400` 全部退化（不互异或 `≤n`），`n=2000` 才五个都 `>n` ⇒ 语料里
`n≤384` 的有限回归根本没测到 FQ/T5**；**A12 真实核五点权重在 `n=1000/2000` 不全为正、`6000/20000` 才全正**，
标准化矩对高斯矩的偏差按 `n^{−1/2}` 衰减（实测 `0.2355→0.1290` vs 预测 `0.548`）。

**两个采样坑（写进正本了）**：① 五点权重**必须**在标准化变量 `s` 解；在 `u` 里解（节点集中在 2.0089、
跨度 1e−3）Vandermonde 条件数 ≈1e13，60 位精度也给不出有效数字 —— 我第一版就这么踩了，重做后才敢下结论；
② 语料的 `check_gaussian_five_point.py` **只**验抽象高斯表 + `a` 的有理区间，**不**算真实核矩，
所以 GC/FQ 的有限证书覆盖的是极限常数、不是收敛性。

**未做**：没改任何数学结论、没动平台、没发提案、没写 Lean。

## ζ(9) Zenodo v0.1 发布包（本轮只做包，不上传）

- 产出目录 `release/zeta9-v0.1/`：PDF（13 页，Chrome 无头打印 HTML+MathJax）、
  `zeta9-source-v0.1.zip`（284 条目 1.41 MB，白名单 `pack_release.py` 打包）、README、CHANGELOG、
  zenodo-metadata、AUTHOR_REVIEW（私有，不进 ZIP）。
- 发布前核查三条更正（数值验证脚本 `release-checks/zeta9_release_check.py`）：
  ① 审计 R19「Eulerian 系数和 360」错，实为 720=6!；原文 analytic.md 用三角形式
  （系数 302+57+1=360 / 分母 360）自洽，脚本不受影响，只改审计文字；
  ② 审计 R20 把 log|R| 的尺度误写成 (1/n)ReΦ，正确是 n·ReΦ+O(log n)（与 R21/原文一致）；
  ③ f_*<−τ（−f_*=10.5641538>τ=10.564，裕量 1.54e-4）；审计两处写反方向，DAG.md 原文是对的。
- 环境：本机无 LaTeX/pandoc；HTML→PDF 用
  `chrome.exe --headless=new --no-pdf-header-footer --virtual-time-budget=30000 --print-to-pdf=... file:///...`
  （Edge 也在）。MathJax CDN 渲染正常。
- 打包脱敏：绝对路径、平台 UUID/短 ID 在 staging 副本替换；扫描的「手机号」命中全是
  SHA 片段/大整数误报。gram-coeff(1.5M) 与 platform/wiring 未入包；linear-coeff 入了。
- 待宇轩确认：署名形式、机构、ORCID、许可证、审计原文带错入包是否可接受。未上传、未建 DOI。

## ζ(9) 发布包第二轮：审阅修订 + 身份/许可确认（仍未上传）

- 作者确认：署名 **Yuxuan Xu**（family Xu / given Yuxuan）、机构 School of Computer Science,
  Wuhan University、ORCID 0009-0001-8760-5277（校验位已核）、文稿 CC-BY-4.0 + 原创代码 MIT
  （LICENSES/ 两份文本已入 ZIP，README 加 Licensing + How to cite，DOI 待发布后回填）。
- 应用审阅 7 项重措辞（CHANGELOG R-V~R-XI）：§5 上界措辞改准确；§10 系数 0.19→0.055
  （1/√330.658=0.05499337）且 n=2000 限定为离散测试点；删「阈值位于 (2000,6000]」；
  §11 不再声称分解严格强于根；§12 拆数学义务 vs 形式化任务；§13 弱化方向表述；
  元数据 R8 加 sketch 限定。
- ⚠️ 工具坑：并行 Edit 同一文件会丢编辑（首轮标题页/§10b/§11 三处未落盘但报成功）。
  同一文件的多个编辑必须串行发；改完用 grep 复核落盘。
- 重打包 286 条目 / 1.42 MB，PDF 重印 13 页；13 项修订点自动检查 + 首页截图复核全过。
  AUTHOR_REVIEW.md 身份许可项已勾选，内容通读项仍待作者。未上传、未建 DOI。

## Zenodo v0.1 已发布（用户本人操作）

- 记录 https://zenodo.org/records/22951155，DOI `10.5281/zenodo.22951155`，
  CC BY 4.0（文档）+ MIT（代码），含 PDF + source zip + README + CHANGELOG。
- 后续版本机制已向用户说明：已发布记录的文件不可替换，v0.2 走新版本（同一 concept DOI）。
- 已建议并已执行：单开公共仓库 https://github.com/Anchen0823/zeta9-research-notes
  （本地 `D:\users\self_projects\zeta9-research-notes`，从 `release/zeta9-v0.1/staging` 初始化）。
  README 回填 DOI：concept `10.5281/zenodo.22951154` / v0.1 `10.5281/zenodo.22951155`。
  新增 `formalization/Zeta9Note.lean`=8 定理自包含单文件（`lake env lean` exit 0、无 sorry），
  + `docs/{statements,explanations}/`。issue #1–5 已建（math/lean/release 标签）。
  ✅ Zenodo–GitHub 联动已开（用户）；✅ Project #2 已接线（token 加 `project` 后，
  5 个 issue 已 `addProjectV2ItemById` 入板，板名 "@Anchen0823's Math Research"，
  project node id `PVT_kwHODN2lvs4Bknqe`，Status 字段 Todo/In Progress/Done）。
  ⚠️ 发 v0.2 时别再手工传 Zenodo —— 直接在 GitHub 发 release，联动会自动归档。

## 公开 ResearchPaper mission（Draft 就绪，待用户 Submit）

- 目录 `missions/zeta9/roadmap/platform/public-proposal/`（含 proposal.json / description.md /
  statements/ / solutions/ / readbacks/，脚本 `build_public_proposal.py`）。
- 平台 proposal id `00d787d8-91c8-45bf-87f0-6a5e9e8bc472`，ResearchPaper + public，
  8 个 draft items（namespace `Zeta9Note`，与私有 mission 的 `ZetaNine` 隔离），main = one-form
  criterion，7 milestones，盲审全 FAITHFUL，读回校验通过，状态 **Draft**。
  ✅ **用户已 Submit**：状态 **In review**，visibility public，mission_id 待版主批准。
  查状态：`python missions/zeta9/roadmap/platform/public-proposal/build_public_proposal.py --status`
- 排除项：ExponentialOne/TwoFormObligation + TwoFormObligation（含 `riemannZeta 9`，私有重叠）。
- 关键坑：平台 draft item 的 Lean 文件必须第一行 `import Mathlib` 且**单行** `:= by sorry`
  （`:= by\n  sorry` 会被 `draft_body()` 判 layout 错）；归约型证明靠本地镜像
  `Theorems/Thm_<Ns>_<name>.lean` + `import Theorems.Thm_...`，镜像要先 `lake build` 出 olean。

## GitHub Project #2（数学追踪板）

- 用户建了 https://github.com/users/Anchen0823/projects/2 记录数学相关进度，**私有**。
- gh CLI（keyring，`gho_` OAuth token）scopes 只有 gist/read:org/repo/workflow，
  **缺 `read:project`** ⇒ GraphQL 读不了板；匿名访问 404。要看板需 `gh auth refresh -s read:project`
  或改公开。agent-browser 未安装、无 playwright 缓存。
- 已给规划建议：Project=追踪层，repo=zeta9 研究档案家（v0.1 zip 初始化）；issue 从 audit 的
  open nodes 生成；保持轻量（爱好不是 KPI）。
