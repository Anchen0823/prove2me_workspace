# Session checkpoint

Work stopped at the user's request because their quota is exhausted.

- The Sondow integral identity is Proved on Prove2Me with an empty frontier;
  see `sondow/INTEGRAL-IDENTITY-COMPLETE.md` for accepted submission evidence.
- The scaled integral bound and the infinite-range Schoenfeld estimate remain
  unproved. Do not report the conditional assembly as an unconditional proof.
- The source PDF has been supplied and inspected. Do not request it again.
- Numerical threshold, three-row coverage, tail estimate, and conditional
  assembly are checked locally with standard axioms. The final assembly build
  took 74.8729042 seconds including dependency handling; no platform submission
  was made for it. See `sondow/INFINITE-RANGE-STATUS.md`.
- Each theorem must pass platform verification within 300 seconds.
- Use English artifacts and Chinese reports. Sol, Terra, and Luna delegation
  is authorized for bounded tasks, with parent review and intervention.

This checkpoint excludes temporary files, credentials, compiled outputs, and
generated files larger than 10 MiB. The large finite-certificate experiments
remain on disk; their generator, hashes, outcomes, and limitations are recorded
in `sondow/ROSSER-FINITE-CERTIFICATES.md`. The million-prefix certificate is
locally proved but took over 300 seconds and does not solve the full leaf.
Unrelated five-primes work is left outside this scoped commit.
