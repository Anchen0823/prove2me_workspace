The proof establishes the actual odd rectangular scale estimate for both numerator signs. It derives a finite Hilbert bound, controls the cosecant kernel, expands the exact geometric Gram matrix, and applies the resulting finite large-sieve inequality to consecutive blocks of half-indices. Ceil/floor endpoints and zero padding are proved to preserve the literal sum and coefficient energy. The actual Mobius and public centered coefficients satisfy the required energy budgets, yielding the exact 1.1/8 prefactor. Conjugation handles a=-1.

This is a complete proof: it imports public definitions and Mathlib only, with no Open theorem assumptions. The finite spectral identity retains its upstream Apache-2.0 attribution in the source. Local verification prints only propext, Classical.choice and Quot.sound for theorem solution.
