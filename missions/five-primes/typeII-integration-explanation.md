This completes the remaining scale-integration implication for the public Type II sum. The proof first turns the actual double tsum into a finite natural rectangle, using the support of eta_0. It proves eta_0(d*w/x) equals four times the logarithmic length of the intersection of the two scale windows, proves integrability, and interchanges the finite sum and integral. Explicit odd natural/integer reindexing identifies the resulting kernel with the public scale sum divided by W. The exact support interval is [V,x/U].

The supplied pointwise estimate is then integrated. The unit parameter regime x<=qW provides the coupled radical bound; the logarithmic term is integrated exactly against dW/W, and the two root terms use their explicit primitives and log(W)<=log(x/U). Positive square-root identities normalize the endpoint factors. The previously verified numerical inequalities give 0.1, 0.39, 0.55 and 0.78 with precisely the target logarithms.

The submission is a complete proof of this implication, with the pointwise estimate an explicit hypothesis of the target. It imports no Open theorem and contains no proof placeholders. The existing parent Type II sketch discharges that hypothesis using the now-Proved signed scale leaf.
