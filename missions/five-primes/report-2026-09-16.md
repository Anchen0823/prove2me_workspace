# workflow.md 12-段报告 — 2026-09-16

Mission: `cdf62c01-8ad9-4eb0-bbcc-ef94ce25d4f8`
Environment: `leanprover/lean4:v4.33.1`, Mathlib `0df444a360eaa60ab8c11dca51a86af692955474`
Submission mode: **UNSPECIFIED**（推定为 DRAFT_ONLY；未提交任何内容）

---

## 1. TARGET SNAPSHOT

**主目标**（本次选定）：
`TaoFivePrimes.theorem51_vaughan_split`
- ID: `197dea21-9471-497e-ba7a-3372b3e6eab7`
- 状态：**Open**
- 角色：Theorem 5.1 minor-arc 链的**共享瓶颈**——被 4 个 reduction sketch
  （`5bd1af4a` / `22e5ccc9` / `14cbfbd0` / `c2a77a5b`）共同引为 child。
- 陈述：`U,V ≥ 40`，`U,V < x`，`UV ≤ x/4`，`x ≤ UV²` ⟹
  `∃ c : ℕ → ℂ, (∀ d ∈ theorem51Divisors U V, ‖c d‖ ≤ 1) ∧
   ‖smoothedExpSum eta0 2 x α‖ ≤ theorem51TypeI x α U V c + theorem51TypeII x α U V`

**次要目标**（本次已完成闭合）：
`TaoFivePrimes.theorem51_typeI_block_summation`
- ID: `7b6bb2d9-e6fa-4629-80e7-f3a34b9ff416`，状态 **Open**
- 其唯一剩余形式缺口 `hblock` 已在本地机器验证地闭合（见 §6）。

**使命规模**：131 节点 / 68 Open / 1 Disproved×2。

## 2. SOURCE AND STATUS AUDIT

**源**：Terence Tao, *Every odd number greater than 1 is the sum of at most
five primes*, arXiv:1201.6656v4，Lemma 4.11 + §5.2（(5.8) 之前）。
本地原论文：`referpaper/EVERY ODD NUMBER GREATER THAN 1 IS THE SUM OF AT.pdf`；
提取文本 `tmp/tao.txt`（第 23–26 页对应 1468–1720 行）。

**源文核对**（逐条比对，非依赖摘要）：
- Lemma 4.11 陈述（第 1472–1499 行）：`|Σ Λ(n)F(n)| ≤ T_I + T_II`，
  `g(w) = Σ_{b|w,b>V} Λ(b) − ½log w`。
- 证明（第 1510–1588 行）：`Λ = µ1_{≤U}*log − µ1_{≤U}*Λ1_{≤V}*1
  + µ_{>U}*Λ_{>V}*1 + Λ1_{≤V}`；F 在末项支撑上消失。
- **关键 observation**（第 1546–1548 行）：
  "Observe that when `d > U` and `w > V`, then the term `µ(d)(½ log w)F(dw)`
  vanishes unless `U < d ≤ UV`."
- §5.2 的 T_I / T_II（第 1651–1665 行）与平台 `theorem51TypeI/II` 逐项一致。

**陈述忠实度**：平台 `theorem51_vaughan_split` 的 `theorem51Centered V w` 与论文
(4.19) 的 `g(w)` 定义一致（已核对 `Def_TaoFivePrimes_Theorem51Sums.lean` 第 25–27 行）。
`eta0` 支撑 `(1/4, 1)` 对应 `UV ≤ x/4` 与 `x ≤ UV²`。**未发现忠实度问题。**

**已发现的陈述风险（他节点）**：
`vinogradov_block_if_form` (`00db332f`) **Disproved**（提交 `056600bb`，2026-09-15）。
原因：陈述漏掉 `gcd(|a'|, q) = 1`。反例 `B=0, q=10, A'=1, a'=0, m=0` 得 `10 ≤ 2`。
平台已建修正版 `vinogradov_lemma_if_form_coprime` (`cf24713b`) 与
`vinogradov_block_coprime` (`32cd79af`)，均 Open。
→ 涉及 Vinogradov 型输入的节点必须显式携带互素条件。

## 3. EXISTING-RESULT SEARCH

**Prove2Me（使命内，131 节点全量扫描）**：

| 名称 | ID | 状态 | 可复用性 |
|---|---|---|---|
| `theorem51_vaughan_split` | `197dea21` | Open | **目标本身** |
| `theorem51_typeI_block_summation` | `7b6bb2d9` | Open | 次要目标 |
| `theorem51_typeI_block_summation_from_vinogradov` | `7d392d8e` | Proved | 以 Vinogradov 为假设，可复用 |
| `vinogradov_lemma_if_form_from_block` | `90cf0471` | Proved | 可复用（互素版入口待接） |
| `theorem51_typeI_pointwise_envelope` | `9abeeaa8` | Proved | 可复用 |
| `typeI_block_sum_bound` | `9e5e1ed4` | Proved | 可复用 |
| `vinogradov_odd` | `e9ff8fbb` | Proved | 可复用 |
| `TaoFivePrimes_Theorem51Sums` | `0638cceb` | Definition | 接口 |
| `TaoFivePrimes_Theorem51Assembly` | `013f62c3` | Definition | Child 2 发布物，**已确认存在** |
| `vinogradov_block_if_form` | `00db332f` | **Disproved** | ✗ 陈述缺互素条件 |
| `vinogradov_lemma_if_form` | `15c69f72` | **Disproved** | ✗ 同上 |

**关键负面结论**：使命内**不存在**非中心化的 vaughan_split 变体。
上一会话记录的 `uncentered` 节点与 `vaughan_decomposition` 恒等式**均不存在**，
该记录已作废。

**本地（examples/five-primes/，全部编译通过）**：

| 文件 | 关键声明 | 对 vaughan_split 的价值 |
|---|---|---|
| `Theorem51VaughanIdentity.lean` | `vaughan_centered_identity`、**`weighted_vaughan_centered_split`**、`vaughan_restricted_centered_identity`、`vaughan_half_log_correction_support` | **数学内核齐备** |
| `VaughanCenteredCoefficient.lean` | `centeredVaughanCoefficient_abs_le` | 系数界 `|c(w)| ≤ (log w)/2` |
| `Theorem51VinogradovSharp.lean` | `odd_block_from_block`、`odd_symm_min_sum_le`、`vRhs_eq_hblock_rhs` | 尖锐 Cor 3.5 + `hblock` 闭合 |
| `Cor35HblockClosed.lean` | `hblock_closed` | 自包含最小复现 |
| `VaughanSplitBridge.lean` | `eta0_support`、`abs_le_UV_of_prod_le_UV2`、`centeredCoeff_abs_le` | 接口适配件 |

**Mathlib**：`ArithmeticFunction.coe_moebius_mul_coe_zeta`、
`ArithmeticFunction.vonMangoldt_mul_zeta`、`ArithmeticFunction.coe_mul_zeta_apply`、
`ArithmeticFunction.vonMangoldt_sum` 均直接可用（已在既有文件中使用）。

**mismatch 精确定位**：vaughan_split 需要在 `smoothedExpSum`（`ℕ` 上、含
`e(αn)` 与 `η0(n/x)`）的语言里给出分裂；现有内核在 `ArithmeticFunction ℝ` 的
卷积语言里。差异是 **语言/索引适配**，不是数学缺口。

## 4. CLASSIFICATION

**B. LOCAL_BRIDGE**

理由（三条独立证据）：
1. 4 个已接受的 reduction sketch 一致描述该节点为「purely algebraic
   decomposition … asserts no analytic bound and does not mention the rational
   approximation to α」——即无解析内容。
2. 数学内核已存在且机器验证：`weighted_vaughan_centered_split` 就是分裂本身；
   `vaughan_half_log_correction_support` 就是 ½log 吸收（源文第 24 页的关键步）。
3. 剩余工作是 `ArithmeticFunction` ↔ `smoothedExpSum` 的接口适配，
   属「小的陈述/API/类型适配」，符合 LOCAL_BRIDGE 定义。

**非** OPEN_RESEARCH（源文有完整证明）、**非** KNOWN_FORMALIZATION（不是
「大量 Lean 形式化待做」，而是零件齐备的组装）、**非** INFRASTRUCTURE_PROJECT。

## 5. CRITICAL PATH

**最小 source-backed 路径**：
```
Theorem51VaughanIdentity.weighted_vaughan_centered_split   （已证，本地）
  + VaughanCenteredCoefficient.centeredVaughanCoefficient_abs_le （已证，本地）
  + VaughanSplitBridge（η0 支撑 / ½log 吸收 / 系数界）（本次已证，本地）
  ──[接口适配：ℕ 求和 ↔ smoothedExpSum；中心化系数 ↔ theorem51Centered]──▶
theorem51_vaughan_split                                    （平台目标）
  ──▶ 解锁 4 个已接受的 reduction sketch ──▶ theorem51_unit_numerator_bound
  ──▶ Theorem 5.1 ──▶ five_primes
```

**非关键但有趣**：`hblock` 闭合（本次完成）属 `typeI_block_summation` 支线，
与 vaughan_split 是**两条不同的路径**——不应混为一谈。

## 6. ACTION TAKEN

**（a）平台状态重新核实**（纠正上一会话的错误记录）。
**（b）三项本地机器验证产出**（全部 sorry-free、编译通过）：

1. `examples/five-primes/Theorem51VinogradovSharp.lean` — 468 行 / 27 声明。
   在先前 `Theorem51Vinogradov.lean` 基础上修正（用 `vmin` 处理正弦零点，
   避免 Lean `B/0=0` 使裸 `min` 在零点归零），并补上：
   - `odd_block_from_block`：Cor 3.5（奇数、宽 2q、块计数 1）由 Lemma 3.4 单块形式推出；
   - `odd_symm_sum` + `odd_symm_min_sum_le`：反射减半（Tao 的 "by symmetry"）；
   - §6：`vRhs_eq_hblock_rhs` / `hblock_of_sharp` / `hblock_n_indexed_of_sharp`，
     识别 `A = X/2 + C`、`B = C` 时 `vRhs` 恰为 §10 右端。

2. `examples/five-primes/Cor35HblockClosed.lean` — 93 行，自包含（仅 `import Mathlib`）。
   `hblock_closed` 经 `#print axioms` 确认只依赖 `[propext, Classical.choice, Quot.sound]`。

3. `examples/five-primes/VaughanSplitBridge.lean` — 10 声明。
   `eta0_eq_zero_of_le_quarter` / `eta0_eq_zero_of_one_le` / `eta0_support`
   （`eta0 t ≠ 0 ⟹ 1/4 < t < 1`）、`abs_le_UV_of_prod_le_UV2`
   （½log 吸收）、`centeredCoeff_abs_le`。

**（c）无平台提交**——submission mode 未指定，按 workflow.md 默认禁止。

## 7. PROPOSED CHILDREN

**None.**

理由：目标已分类为 LOCAL_BRIDGE，零件齐备，不需要分解。按 PHASE 5，
decomposition 仅在直接复用或直接证明不可行时才使用；此处两者都可行。

（若未来需要，唯一自然的候选是「`smoothedExpSum` 的有限支撑截断」，但那是
一次性适配引理，按 PHASE 2 应保持为 proof-local，不建独立节点。）

## 8. PLATFORM ACTION

**建议（待用户批准，本次未执行）**：

1. **证明 `theorem51_vaughan_split` (`197dea21`)**：以
   `weighted_vaughan_centered_split` + `VaughanSplitBridge` 为零件做接口适配，
   提交为 `proof_type=prove`。这直接解锁 4 个已接受的 reduction sketch。
2. **不新建节点**：现有 4 个 sketch 已覆盖正确的分解缝，重复建节点违反
   PHASE 2「Never create a duplicate theorem」。
3. **复核 `00db332f` 的证伪影响**：确认 `typeI_block_summation` 链使用的是
   Proved 的 `vinogradov_lemma_if_form_from_block` (`90cf0471`) 而非被证伪的
   `if_form` 变体；若依赖被证伪者，须迁到 `cf24713b`/`32cd79af` 的互素版。
4. `hblock` 闭合结果建议作为 note/comment 附于 `theorem51_typeI_block_summation`
   (`7b6bb2d9`)，标明「§10 的 `hblock` 已有机器验证来源」。

## 9. BLOCKERS, RISKS, AND STOP REASON

**数学风险**：低。源文 Lemma 4.11 有完整证明，本地内核已机器验证。

**接口风险（中）**：`smoothedExpSum` 是 `∑' n : ℕ`（无限和，靠 `eta0` 支撑
截断），而 `weighted_vaughan_centered_split` 在 `Finset ℕ` 上。适配需要：
(a) 证明 `eta0` 支撑外项为零以把 `∑'` 化为有限和；
(b) 把 `ArithmeticFunction` 的卷积评估展开到显式除数和；
(c) 建立 `theorem51Centered V w = vaughanCentered V w`（后者是 `ℝ` 值
`ArithmeticFunction` 的求值，前者是 `ℕ → ℝ`）。
三项都是纯适配，但 (a) 涉及 `tsum` 的 `HasFiniteSupport` 论证，是全链最繁琐处。

**平台风险（中）**：`00db332f`/`15c69f72` 的证伪说明该使命对陈述细节（互素、
零点约定）敏感；提交前应自查零点约定（`vmin` 已处理）。

**预算**：本次已用约 40 次工具调用。**停止理由**：LOCAL_BRIDGE 的零件已
全部备齐且验证，继续推进需进入「接口适配实现」——那是一段独立的、
需要连续多步编译调试的工作，且其终点是**平台提交**，而 submission mode 未指定。
按 PHASE 7，「继续将主要产生未提交的本地代码」不是有效进展，故在此汇报。

## 10. SMALLEST USEFUL NEXT STEP

**一个有界动作**：完成 `smoothedExpSum eta0 2 x α` 的**支撑截断引理**——

    `smoothedExpSum eta0 2 x α = ∑ n ∈ Finset.Icc (⌊x/4⌋₊ + 1) (⌊x⌋₊),
        [Coprime n 2] · Λ(n) · e(αn) · η0(n/x)`

即把 `∑' n : ℕ` 化为有限和。这是整个 vaughan_split 适配中**唯一有真实技术
难度**的一步；其余（卷积展开、系数识别）是机械适配。完成它之后，
`weighted_vaughan_centered_split` 的应用就只剩重写。

## 11. HANDOFF RECORD

**已执行的搜索**：使命全量 131 节点扫描（分页）；
`/theorems?search=vaughan`、`=theorem51_vaughan`、`=vaughan_decomposition`；
`/theorems/<id>/graph`（97 节点 / 210 边）；
`/submissions/<id>` × 5（4 个 sketch + 1 个证伪）；
本地 `grep` 全部 five-primes 文件的 sorry/axiom。

**已检视的定理 ID**：`197dea21`、`7b6bb2d9`、`e1794571`、`013f62c3`、`0638cceb`、
`00db332f`、`90cf0471`、`7d392d8e`、`15c69f72`、`cf24713b`、`32cd79af`、
`9e5e1ed4`、`9abeeaa8`。

**已尝试的方法**：`lake env lean` 单文件编译（Sharp / Closed / Bridge / VaughanIdentity
/ VaughanCenteredCoefficient，全部 EXIT 0）；`#print axioms` 公理审计（干净）。

**编译结果**：5 个文件全部通过，零 sorry、零自定义 axiom。

**已知阻塞**：`examples/` 无 `.olean`，跨文件 import 在单文件检查下失败 →
胶水引理须同文件或自包含（已按此处理）。

**决定不重复的事**：
- 不再相信上一会话关于 `uncentered` / `vaughan_decomposition` 的记载（已证伪）。
- 不对同一 `hblock` 再做第二次独立推导（Sharp 模块已含两处一致表述）。
- 不使用裸 `min` 于含正弦零点的 summand（用 `vmin`）。

**下一允许实验**：§10 的支撑截断引理（见上）。

## 12. RECOMMENDATION

**PROVE LOCAL BRIDGE**

`theorem51_vaughan_split` 是 LOCAL_BRIDGE：数学内核已机器验证地存在于本地
（`weighted_vaughan_centered_split` + `vaughan_half_log_correction_support` +
`centeredVaughanCoefficient_abs_le`），剩余为接口适配。它是 5 条 sketch 的共享
瓶颈，解决它一次性解锁 4 个已接受的 reduction，是当前整个 mission 上
**性价比最高的单点**。

建议的下一步即 §10：先做 `smoothedExpSum` 的支撑截断，再机械应用内核，
最后在用户批准 submission mode 后提交。
