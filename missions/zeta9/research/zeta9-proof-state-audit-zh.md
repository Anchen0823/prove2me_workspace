# ζ(9) 成果整理：证明状态审计（中文摘要）

2026-09-25。**结论：本轮工作没有任何地方可以声称无理性结果。** 本文是
[英文 LaTeX 正本](zeta9-proof-state-audit.tex)（`zeta9-proof-state-audit.tex`）的中文提要：
正本分 **Part I（第一轮）** 与 **Part II（九轮全景 + roadmap + 平台层）**，
逐条分类、画出依赖图、列出全部逻辑缺口，**不做任何修补**。细节以正本为准。

## 0. 一句话

九个回合把「精确代数 / 全素数整化 / 定值积分 / 有限 Arb 验证 / 两套无理性准则 / 一轮全 $n$ 非零定理」
都做了出来，但**通往上界的路走完了、通往下界的路一步没走**：
Part I 只拿到 `|L|` 的**上界**（`limsup log|L|/n ≤ sup H_α ∈ (0.67, 2.80)`）与分母的**上界**
（`log m_prim/n ≤ 9−Γ(α) ≈ 8.15–8.86`）；roadmap 的 13 个开放节点全部是 `∀`（对所有 primitive 方向、或无穷多 `n`），
而语料里只有 5 个尺度的有限证书。**没有任何一条 c > 0 的 `exp(−cn)` 由整数形式给出。**

## 1. 状态标签

| 标签 | 含义 |
|---|---|
| **R** | 有书面证明，逐条查过未发现错误（**不是**机器验证） |
| **R\*** | 依赖已声明的标准输入（PNT、Legendre、von Staudt–Clausen、Euler 偶 ζ 公式） |
| **M** | 只在前一条引理（含 zeta7 既有部分）成立时成立 |
| **N** | 仅有限区间/精确算术验证，无一致性 |
| **H** | 启发式，不是证明步骤 |
| **OPEN** | 需要但完全缺失 |

## 2. 九轮一览（每轮改了什么 / 关了什么 / 留了什么）

| 轮 | 改动 | 关掉 | 留下 |
|---|---|---|---|
| 1 | 三阶极点 + 六次导数 → 单 ζ(9) 线性形式 | 精确代数、两套整化、乘子渐近、解析上界 | `A≠0`、非零性、衰减、真实分母、鞍点 |
| 2 | 极点阶 `p∈{3,5,7,9}` + 余子式消元（种子 `(-7776,1701,-224,1)`） | ✅ **全语料第一个指数衰减**：四 ζ 值形式 `0<F_n≤e^{(−1.43+o(1))n}` | 消元矩阵满秩、消元后 `A₉≠0`、单 ζ(9) 形式非零衰减 |
| 3 | 有理移位 `a/D` + 固定除数权重（`κ₆=6531840`、`κ₈=92897280`） | 权重恒等式、`A=κ_Dρ₉>0`、尾分支 `f(x_*)<−10.43`、全素数证书 | 统一 primitive 下界（31 例 primitive 全 `>1`） |
| 4 | 最高系数 `ρ_n` 全序列非零 + 模 `p` 截断残数 | ✅ **全语料最强非零性**：`sgn(ρ_n)=(−1)^{n/2}`、`ρ_n≠0` 对全部偶 `n`；`v_p(B)=−9`（4 例 `=−8`，2428 参数-素数对） | 非零截断残数与素数的统一加权密度（条件性障碍定理） |
| 5 | 整数多项式权重 `W(u)`，`u=t(t+n)`，`deg≤4` | 全核分类、固定次数归约、双层停止判据（净成本 `≥0.75>0`） | 无穷子序列上的统一构造 |
| 6 | 用**真实 primitive 乘子**选向 + Smith 型 | primitive 坐标引理、Smith 判据、精确 lift 定理；**认证 `r_n = −5.10…/−5.06`（n=12…192）**；`n=192` 得 `b>10^{422}` | `λ₂` 的统一上界（准则 `limsup<10.43` 未证） |
| 7 | 连接定理 `F_{n+2}=C(n)F_n` + `det F_n` 闭式 | 全偶 `n` 连接定理；`N ∣ d_n^{45}d_{7n/2}^{8}`；实数侧锐化 `−10.564155<f_*<−10.564`；5 个尺度加权 `λ₂` 全部 `<10.564` | ✅ 已证的**方法级障碍**（全空间传递范数成本 `≥−f_*`），以及无穷 `n` 的加权 `λ₂` 界 |
| 8 | 冻结极限模型 | `G=M^{-1}` 特征多项式不可约；固定块长无可剥离的不变子空间；`deg≤4` 滤波不能指数消去；`ρ=e^{21.128…}` | **明确不是**真实变系数递推的 no-go；建议停止同规模升级 |
| 9 | 次数随 `n` 增长的滤波 | `|P(ρ)|≥a^{−d}((d+1)HR^d)^{−4}`（`a=22235661`、`R=3709731751`）⇒ 次线性次数/次指数高度滤波无效 | 次数 `Θ(n)`、高度 `exp(Θ(n))` 的滤波仍可能，但必须计入算术成本 |

> 读表须知：第 2 轮的衰减是**四个 ζ 值**的形式；第 4 轮非零性是**系数**而非线性形式；
> 第 6/7 轮的负值 `r_n` 与越线都是**有限**的；第 8/9 轮障碍只在**冻结常系数模型**里成立。

## 3. roadmap 本地 DAG（2026-09-25 状态）

对象：`F_n`（行 `u⁰..u⁴`，列 `B,A₃,A₅,A₇,A₉`）、`K_n`（饱和整核基）、`D_n`、Smith 因子 `s₁|s₂`、
`N_n=s₂/s₁`、`g_n=gcd(N_n,d_n²)`、面积 `Δ_n`、`μ_n=μ₁(Λ_{g_n})`、阈值 `τ=2641/250=10.564`，
`B_n=log(D_n/s₁)+½logΔ_n−½log g_n)/n`、`σ_n=(½log(g_nΔ_n)−logμ_n)/n`，
**精确恒等** `B_n+σ_n = [log(D_n/s₁)+logΔ_n−logμ_n]/n`。

**40 个节点：13 OPEN + 27 「笔记已证」**（后者**无 Lean 定义、无平台定理**）。

- **OPEN**：`J`（`B_n+σ_n≤τ−ε` 无穷次，真正的目标）、`V`（`limsup B_n<τ`）、`S`（`σ_n→0`）、
  `VA`（`<τ−γ`，`γ=¼log(3 711 015 000)≈5.50864282`）、`VB`（`<1.11071437`）、`VC`/`VD`（`<0.61071437`）、
  `CP`、`T`、`TS`、`T5`、`TG`、`FD`。
- **笔记已证**（节选）：`CM`（**全语料唯一的等价判据**：
  `ζ(9)∉ℚ ⟺ λ_{2,n}=o(T_n) ⟺ liminf λ_{2,n}Z_n=0 ⟺ λ_{1,n}/(Ξ_nZ_n)→∞`）、`X`、`A`、`F`、`P`、`TP`、`SP`、
  `FQ`、`GC`、`FI`、`FC`、`FO`、`XL`、`TA`、`Q`、`Y`、`YD`、`H`、`Z`、`I`、`M`、`C`、`L`、`LC`。
- **已证归约**：`T5∧FQ∧X∧A∧F ⇒ R`（`T/TS/TG` 同理）、`CP+CM ⇒ R`、`V∧S ⇒ J`、`J ⇒ CP`、
  `FD+FC+FI ⇒ T5`、`VA+Y ⇒ V`、`VB+Y ⇒ VA`、`VC+H+Z ⇒ VB`、`VD ⇒ VA`。**13 个 OPEN 一个没证。**
- **已证否定结果（4 个，均非 no-go）**：`GO`（只靠二维+面积+短度推不出 T5）、`FM`（同谱极限+正性+五点精确求积
  仍可让 T5 永远失败，但不保留真实 `C(n)`）、第 7 轮谱障碍、第 8/9 轮滤波障碍。
- **算术链的数值距离**：`V` 需要 `limsup B_n<τ`；已有最紧的统一估计是
  `limsup(𝓔_n^mid−𝓤_n)/n < 22.337`，而要求 `< 0.61071437` —— **差 30 倍以上，且方向不利**。
  5 个有限参数越线（`B_n+σ_n ≈ 10.4933, 10.4423, 10.4767, 10.1416, 10.2703`，低于 τ 约 0.07–0.42），
  roadmap 自己三处声明有限越线推不出任何东西。

## 4. 平台层（Prove2me private mission）

| 项目 | 标识 / 状态 |
|---|---|
| 提案 / mission | `06faf23f-…` / `8195d8fe-059f-4515-92b4-57b585e2bac6`，`Private` |
| 根定理 | `ZetaNine.irrational_zeta_nine` = `523f37b8-…`，回读 **Open** |
| 根分解（两条） | `42bd3e13`（单形式）/ `0da22fdd`（双形式），均 `SKETCH_ACCEPTED` |
| 已接受的一般归约 | `irrational_of_two_small_integer_forms`、`exponential_margin_of_volume_and_shape`（本地 Lean 4.33.1 编译） |
| 已接受的抽象核心 | `volume_baseline_cancellation`(`d6b0eb73`)、`finite_prime_budget`(`8496d3a0`)、`finite_prime_discount`(`0f7bb5e3`) |
| 2026-09-25 新接线 | `irrational_of_small_nonzero_integer_forms`(`c1147ec6`)、`taylor_sign_implies_kernel_sum_pos`(`a8a15e54`)、`quadrature_exact_of_moments`(`443fb397`) 均 Proved；`exponentially_small_nonzero_forms...`(`60c1cf49`)、`...independent_forms...`(`691b1665`) 均 Open |
| 里程碑 | 40（原 35），32 条研究里程碑已回读；5 条新陈述盲审均 FAITHFUL AND WELL-POSED |

**故意没接线的**：`F_n`、`K_n`、Smith 数据、加权面积、同余格极小值**都没有 Lean 定义**，
所以 `V←VA,Q,X`、`VA←VB,Y`、`VB←VC,H,Z` 等算术箭头**没有**作为平台分解发布。两条原因（见 `wiring/README.md`）：
① 「存在小整数形式」型孩子一旦有准则就与根**等价**，是伪装的重述 ⇒ 现在两个义务钉了**固定指数速率**、
严格强于根；② 带自由参数的父节点无法 import 封闭的孩子定理，多级图必须先有定义层。

## 5. 本次审计的独立复核（可复现）

脚本与原始日志：`missions/zeta9/verification/audit-2026-09-25/`。

| 复核 | 结果 |
|---|---|
| A1 | `(s,n,m)=(9,2,0)` 从零重算部分分式：`A=−168`、`B=176.228515625`、`L=7.89110563021818797780073476895…`，与归档直接求和证书在其包络精度内一致 |
| A2 | 轮廓恒等式定向符号：`I=−49.5812789531890297i`，`−I/(2πi)=L` ⇒ **(C1) 负号正确** |
| A3/A4 | `H′` 闭式（C6）与闭式数值导数一致到 `~10⁻⁴⁰`；`H″` 二次式证书 6/6 吻合，主系数 `=3−14α` |
| A5/A6 | 独立复算 `Γ(α)` 6/6 落在归档区间内；`sup H_α` 6/6 与归档严格上界一致到 12 位且低于归档 |
| A7 | 合并上界：归档有理数均大于实算值、等于其 6 位向上取整，**全部为正** |
| A8 | 更强整化 `t♯=d_n⁹/G` 在 5 组参数（含 `n=224,m=48`）从零复核：`G`、`d` 与记录一致，`t♯A,t♯B∈ℤ`，`t♯/m_prim` 等于归档整数 |
| A9 | 大素数 `v_p(G)` 公式 + Legendre 式在新数据上：216 组参数、1951 素项、**0 失败** |
| **A10** | 从构造独立推出 round5–7 的相位 `f(x)=10x log x−(x−1)log(x−1)+(x+2)log(x+2)−10(x+1)log(x+1)`，得 `x_*=1.00297646575489808`、`f(x_*)=−10.5641538376402`、`a=330.65816828687`、`√(3/a)=0.09525131927` —— **逐位复现笔记中的全部这些常数** |
| **A11** | FQ 五个预定点 `k_{n,j}=⌊nx_*+j√(n/a)+1/2⌋`：`n=12` 五点全同；`n=192` 为 `{191,192,193,193,194}`（不互异且有 `≤n`）；`n=384` 互异但有 `383<n`；`n=1400` 含 `=n`；**直到 `n=2000` 才五个都 `>n`** |
| **A12** | 真实核五点求积（在良态标准化变量 `s` 中解）：控制组（节点 `−2..2` + 高斯矩）**精确**给出 `(1/12,1/6,1/2,1/6,1/12)`；真实矩下权重 `n=1000` 与 `n=2000` **不全为正**，`n=6000`、`n=20000` 才全正；标准化矩与高斯矩的偏差按 `n^{−1/2}` 衰减（实测 `0.2355→0.1290`，与 `3.33^{−1/2}=0.548` 吻合） |

**复核不覆盖**：R8 的**证明**（只验结论）、R21 的一致性常数、Gram 侧算术、平台层任何定理、以及 C1–C10 全部。

## 6. 六项指定审计的结论

1. **整性/分母**：Part I 已闭合到 `d_n⁹/G`（A8 复核通过）；但**真实分母 `m_prim` 的渐近缺失**，
   `9−Γ(α)` 严格说是「已证可行乘子」的成本上界。roadmap 侧：`N_n` 的支持集已证（第 7 轮），
   但 `FD` 的**求值后**端点分母未知，`VC/VD` 的中间素数超额仍是 22.337 vs 0.6107。
2. **非零性**：第 4 轮的 `ρ_n≠0`（全部偶 `n`，符号 `(−1)^{n/2}`）是**全语料最强非零性**；
   `SP/FQ/FI` 给「有符号模式 ⇒ 核和严格非零」；但**符号模式本身（T5/T/TS/TG）全开放**，
   Part I 的 `A≠0` 亦缺。
3. **p 进估值**：Part I 的 `v_p(C_j)`、`v_p(G)` 公式闭合（A9 独立复核）；第 4 轮给出 `v_p(B)=−9/≥−8`；
   roadmap 的 `H/Z/I/LC/YD` 为笔记已证；`VC/VD` 开放，且**局部反例已否定**先前两个逐点估值候选界。
4. **渐近**：`X`（`Ξ` 指数 `<γ`）、`XL`（下界率）、`FO`、`TA/FI`（窗宽 `<−10.1109`）、第 7 轮 `f_*` 区间、
   `CM` 的 `Z_n∼C_Z/T_n` 均为笔记已证；`V`/`S` 的联合仍未证；FQ/GC 的**定量有效尺度**见 A11/A12。
5. **指数常数符号**：Part I 全部符号逐项查过（A2–A6）；roadmap 侧查了 `f_*`、`a`、`τ`、`γ`、
   `f_*= −10.5641538… < −τ`（**余量仅 1.54×10⁻⁴/单位 n**，是真证明但余量极小）、`ρ=e^{21.128}`、
   `GO/FM` 的反例模式、以及 FI「端点**允许**取零、不能写成避开闭区间」的修正。
6. **O(·) 一致性**：这正是全部卡点。`I/LC/YD` 的一致性只在「已找到的方向」上；`S` 要求**每个** primitive 方向；
   `J/T5/T/TS/TG/FD` 要求**无穷多个** `n`；`V/VA/VB/VC/VD` 要求**沿所有**偶 `n`。
   5 个尺度的有限证书在语料中被反复声明**不能**替代这些全称量词。

## 7. 未闭合清单（C1–C10，全部 OPEN）

C1 真实 primitive 分母的渐近或匹配下界；C2 `|L|` 的下界（或带相位鞍点主项）；C3 `A≠0`；
C4 `0<|P(ζ9)|≤e^{−cn}` 的合流；C5 Gram 侧一致性 + 独立认证的位势常数；
C6 给 `F_n,K_n`、Smith 数据、加权面积、同余格极小值的 Lean 定义；
C7 几何侧任一节点 `T5/T/TS/TG/FD` 或 `CP`（+ 形式化 `FQ/GC/TA/FI/FC/FO/XL/TP/SP/CM`）；
C8 算术侧 `J` 或 `V∧S`（经 `VA/VB/VC/VD`）；
C9 **任何一个**无穷集合上的全称命题（九轮共同缺失的同一件东西）；
C10 笔记已证层（roadmap 27 个节点 + Part I 笔记证明）的形式化。

**没有任何一个被关闭；因此不主张任何无理性结论。**

## 8. 复现

```powershell
$py = 'C:/Users/anche/.workbuddy/binaries/python/versions/3.13.12/python.exe'
& $py missions/zeta9/verification/audit-2026-09-25/contour_sign_check.py
& $py missions/zeta9/verification/audit-2026-09-25/analytic_constants_check.py
& $py missions/zeta9/verification/audit-2026-09-25/arithmetic_core_check.py
& $py missions/zeta9/verification/audit-2026-09-25/roadmap_layer_check.py
& $py missions/zeta9/verification/audit-2026-09-25/fq_gc_check2.py
```

（需要 `tmp/zeta7/exact_packages` 的 `mpmath`；脚本自行加入 `sys.path`。）
LaTeX 正本：`missions/zeta9/research/zeta9-proof-state-audit.tex`（`pdflatex` 编译两次，需要 TikZ）。
⚠️ A12 复现注意：五点权重**必须**在标准化变量 `s` 中解；在 `u` 里解（节点集中在 `2.0089`、跨度 `10⁻³`，
Vandermonde 条件数 `≈10¹³`）即使 60 位精度也给不出有效数字。
