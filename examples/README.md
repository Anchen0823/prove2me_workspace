# Mission Scratch Work

Use one subdirectory per mission: `examples/<slug>/`.

- [Bunkbed archive](bunkbed/README.md).

Scratch modules are not platform imports. Keep standalone submission files in `Solutions/`, and keep logs, explanations, and server verdicts in `missions/<slug>/`. When moving scratch modules, update their Lean import paths and compile successful modules again.
