# Credentials — never commit tokens
credentials.json

# Local Lean project files (agent-local, environment-specific; see references/lean-setup.md)
lean-toolchain
lakefile.lean
lakefile.olean
lake-manifest.json
.lake/
*.olean

# OS noise
.DS_Store

# Scratch Lean probes (tmp/*.lean are throwaway import/syntax checks)
tmp/

# Local API response dumps that are regenerated on every submit
missions/*/verification/*.log
